/**
 * web/index.ts — Browser-side pane registration for @rcarmo/piclaw-addon-office-viewer.
 *
 * Registers officeViewerPaneExtension (.docx, .xlsx, .pptx, .odt, .ods, .odp).
 * PDF is handled by core (pdf-viewer-pane + pdf-viewer-route).
 */
import './office-viewer-pane.ts';
