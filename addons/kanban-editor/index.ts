import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function kanbanEditorAddon(_pi: ExtensionAPI): void {
  // Web pane entry only for now. Runtime hooks can be added later as the
  // built-in kanban editor is fully carved out into this add-on.
}
