import { test, expect } from "bun:test"; import { execFileSync } from "node:child_process"; import { mkdirSync, mkdtempSync, readFileSync, rmSync, symlinkSync, writeFileSync } from "node:fs"; import { join } from "node:path"; import { tmpdir } from "node:os";
import { SourceReader } from "./source.js";
function fixture(){const dir=mkdtempSync(join(tmpdir(),"review-source-"));const git=(...args:string[])=>execFileSync('git',args,{cwd:dir,env:{PATH:process.env.PATH!,HOME:process.env.HOME!,GIT_CONFIG_NOSYSTEM:'1',GIT_CONFIG_GLOBAL:'/dev/null'},encoding:'utf8'}).trim();git('init','-q');git('config','user.name','Fixture');git('config','user.email','fixture@example.invalid');git('config','commit.gpgSign','false');git('config','core.autocrlf','false');return{dir,git,reader:new SourceReader(dir),write(path:string,text:string|Uint8Array){writeFileSync(join(dir,path),text);},cleanup(){rmSync(dir,{recursive:true,force:true});}};}
test("CR-003/004/111/112 saved files are exact UTF8 independent of Git and untouched",async()=>{const f=fixture();try{f.write('file.ts','\uFEFFone\r\ntwo\t😀\r\n');const before=readFileSync(join(f.dir,'file.ts'));const c=await f.reader.capture({path:'file.ts',mode:'source'});expect(c.files[0]?.newText).toBe(before.toString());expect(readFileSync(join(f.dir,'file.ts')).equals(before)).toBe(true);expect(c.files[0]?.fileIdentity).toBeTruthy();mkdirSync(join(f.dir,'outside'));const separate=mkdtempSync(join(tmpdir(),'review-nogit-'));try{writeFileSync(join(separate,'notes.txt'),'notes');const x=await new SourceReader(separate).capture({path:'notes.txt',mode:'source'});expect(x.files[0]?.newText).toBe('notes');await expect(new SourceReader(separate).capture({path:'notes.txt',mode:'staged'})).rejects.toThrow('unavailable');}finally{rmSync(separate,{recursive:true,force:true});}}finally{f.cleanup();}});
test("CR-006 staged and unstaged snapshots remain separate and reads do not mutate Git",async()=>{const f=fixture();try{f.write('a.ts','base\n');f.git('add','.');f.git('commit','-qm','base');f.write('a.ts','staged\n');f.git('add','a.ts');f.write('a.ts','saved\n');const before=f.git('status','--porcelain=v1');const index=readFileSync(join(f.dir,'.git/index'));const staged=await f.reader.capture({path:'a.ts',mode:'staged'}),unstaged=await f.reader.capture({path:'a.ts',mode:'unstaged'});expect(staged.files[0]).toMatchObject({oldText:'base\n',newText:'staged\n',change:'modified'});expect(unstaged.files[0]).toMatchObject({oldText:'staged\n',newText:'saved\n'});expect(f.git('status','--porcelain=v1')).toBe(before);expect(readFileSync(join(f.dir,'.git/index')).equals(index)).toBe(true);}finally{f.cleanup();}});
test("CR-007 root commit untracked deletion and rename are represented without checkout",async()=>{const f=fixture();try{f.write('a.ts','root\n');f.git('add','.');f.git('commit','-qm','root');const first=f.git('rev-parse','HEAD');const root=await f.reader.capture({path:'.',mode:'commit',commit:first});expect(root.files[0]).toMatchObject({oldText:null,newText:'root\n',change:'added'});f.git('mv','a.ts','renamed.ts');const rename=await f.reader.capture({path:'.',mode:'staged'});expect(rename.files[0]).toMatchObject({oldPath:'a.ts',newPath:'renamed.ts',change:'renamed'});f.git('commit','-qm','rename');rmSync(join(f.dir,'renamed.ts'));f.write('new.ts','new\n');const c=await f.reader.capture({path:'.',mode:'unstaged',includeUntracked:true});expect(c.files.some(x=>x.change==='deleted'&&x.oldText==='root\n')).toBe(true);expect(c.files.some(x=>x.change==='added'&&x.newPath==='new.ts')).toBe(true);expect(f.git('rev-parse','HEAD')).not.toBe(first);}finally{f.cleanup();}});
test("CR-009 merge requires an actual selected parent and historical reads preserve checkout",async()=>{const f=fixture();try{f.write('base.txt','base');f.git('add','.');f.git('commit','-qm','base');const main=f.git('branch','--show-current');f.git('checkout','-qb','other');f.write('other.txt','other');f.git('add','.');f.git('commit','-qm','other');f.git('checkout','-q',main);f.write('main.txt','main');f.git('add','.');f.git('commit','-qm','main');f.git('merge','--no-ff','-m','merge','other');const merge=f.git('rev-parse','HEAD'),parent=f.git('rev-parse','HEAD^1');await expect(f.reader.capture({path:'.',mode:'commit',commit:merge})).rejects.toThrow('Select a merge parent');const c=await f.reader.capture({path:'.',mode:'commit',commit:merge,parent});expect(c.base).toBe(parent);expect(c.head).toBe(merge);expect(f.git('rev-parse','HEAD')).toBe(merge);expect(f.git('status','--porcelain')).toBe('');}finally{f.cleanup();}});
test("CR-081 malicious path revisions symlinks invalid encoding and binary are rejected",async()=>{const f=fixture();try{f.write('a.ts','valid');f.git('add','.');f.git('commit','-qm','base');for(const path of ['../escape','/etc/hosts','a\0.ts'])await expect(f.reader.capture({path,mode:'source'})).rejects.toThrow();symlinkSync('/etc/hosts',join(f.dir,'link'));await expect(f.reader.capture({path:'link',mode:'source'})).rejects.toThrow('symlinks');f.write('bad.txt',new Uint8Array([255,255]));await expect(f.reader.capture({path:'bad.txt',mode:'source'})).rejects.toThrow('UTF-8');f.write('binary',new Uint8Array([0,1,2]));await expect(f.reader.capture({path:'binary',mode:'source'})).rejects.toThrow('binary');await expect(f.reader.capture({path:'.',mode:'commit',commit:'--output=/tmp/evil'})).rejects.toThrow('hexadecimal');f.git('add','link');await expect(f.reader.capture({path:'link',mode:'staged'})).rejects.toThrow();}finally{f.cleanup();}});
test("CR-083 bounded source and explicit history pages",async()=>{const f=fixture();try{f.write('a.ts','small');f.git('add','.');f.git('commit','-qm','first');f.write('a.ts','new');f.git('add','.');f.git('commit','-qm','second');const rows=await f.reader.history('a.ts',1);expect(rows).toHaveLength(1);expect(rows[0]?.subject).toBe('second');expect((await f.reader.history('a.ts',1,1))[0]?.subject).toBe('first');await expect(f.reader.history('a.ts',101)).rejects.toThrow('Invalid history');f.write('large.txt','x'.repeat(2*1024*1024+1));await expect(f.reader.capture({path:'large.txt',mode:'source'})).rejects.toThrow('byte limit');}finally{f.cleanup();}});
