import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function yoloVibeAddon(_pi: ExtensionAPI): void {
  // Browser-side compose affordance only. The web entry injects quick prompt
  // buttons into the compose box; runtime hooks can be added later if needed.
}
